# TacoKeeper Twitter bot

## Development

Install the dependencies:

```sh
# Make sure pip is up-to-date
pip install --upgrade pip

pip install -r requirements.txt
```

Run the bot script:

```sh
./tacokeeper-bot.py
```
